{{!< default}}

<div class="gh-outer">
<main class="site-main gh-inner">

    <div class="post-feed gh-feed">
        {{#foreach posts}}
            {{#match primary_tag.slug "gallery"}}
                {{> "loop"}}
            {{/match}}
        {{/foreach}}
    </div>

</main>
</div>
